# Release Notes

## 2026-04-21

### Main Consolidation: Issue #12 + Workflow Dashboard Freshness

- Synced local `main` with `origin/main` and confirmed issue #12 changes are present.
- Added workflow dashboard improvements:
  - Default workflow selection now uses the latest `updated_at` state.
  - Auto-refresh support with configurable interval in the sidebar.
- Preserved existing local generated artifacts without forcing unrelated changes into this release.
